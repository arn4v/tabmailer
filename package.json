{
  "name": "tabmailer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
		"preinstall": "mkdir ~/.npm-global && npm config set prefix '~/.npm-global' && npm i -g pnpm && npx only-allow pnpm",
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^7.4.0",
    "cross-env": "^7.0.3",
    "dotenv": "^9.0.2",
    "node-cron": "^3.0.0",
    "nodemailer": "^6.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
