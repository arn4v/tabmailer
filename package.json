{
	"name": "tabmailer",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node ./src",
		"dev": "cross-env NODE_ENV=development nodemon -w ./src --exec node ./src",
		"format": "prettier --write ./src/**/*.js"
	},
	"dependencies": {
		"better-sqlite3": "^7.4.0",
		"cross-env": "^7.0.3",
		"dotenv": "^9.0.2",
		"node-cron": "^3.0.0",
		"nodemailer": "^6.6.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.7",
		"prettier": "^2.3.0"
	}
}
